```graphql
query getAuctionsInProgress($nowInSeconds: BigInt!) {
    nftMarketAuctions(
      where: {status: Open, dateEnding_gt: $nowInSeconds}
    ) {
    nft {
      tokenId
      tokenIPFSPath
    }
    reservePriceInETH
  }
}
```